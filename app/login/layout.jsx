export const metadata = {
  title: "Secure Login | Thulla Masters",
  robots: {
    index: false,
    follow: false,
  },
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
